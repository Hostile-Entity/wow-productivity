import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.customapp.wowproductivity',
  appName: 'WoW Productivity',
  webDir: 'dist'
};

export default config;
